import {createGlobalStyle} from "styled-components";

export const EstiloCorpo = createGlobalStyle`
    * {
        box-sizing: border-box;
    }
    body {
        font-family: 'Recursive', sans-serif;
        font-weight: 400;
        background-color: #FB6B6B;
        cursor: default;
    }
`